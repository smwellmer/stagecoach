{
  "routes": [
    {
      "method": "GET",
      "path": "/lunches",
      "handler": "lunch.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/lunches/count",
      "handler": "lunch.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/lunches/:id",
      "handler": "lunch.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/lunches",
      "handler": "lunch.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/lunches/:id",
      "handler": "lunch.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/lunches/:id",
      "handler": "lunch.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
